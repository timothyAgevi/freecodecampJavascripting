// instruction
    // Fix the comparison operator in the
    //terminal condition so the loop only runs 
    // for i less than or equal to 4.

    function myFunc() {
        for (let i = 1; i <= 4; i++) {
          console.log("Still going!");
        }
      }